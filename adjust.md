现在需要你将项目进行一个改造，项目主文件是 @main_process.py，大致的内容可以参考 @design.md，请先通读项目，了解整个项目做了什么。请注意 @MobRef文件夹里面不是本项目，而是其余项目，之后的改造你可能可以参考该项目进行改造。

现在做出如下改造：

# 训练模式和测试模式

现在需要你新增训练模式和测试模式，就像 @MobRef中做的一样，两种模式基本共享一套逻辑，但是某些部分出现分支，尤其是涉及强化学习训练和保存时，这样代码比较优美。需要注意的是，测试和训练在 @MobRef中使用不同的数据，而在当前项目是一套，所以，新增参数，决定训练集和测试集的比例，然后按比例，从读取的数据集（现在应该是从数据集里取出一段日期，那么就将这部分日期进行分割，前部分用于训练，后部分用于测试，按比例）。

# 感知层

请按照 @method_design.md 里面的感知层设计，设计感知层。

# RL方法

请按照 @method_design.md 里面的RL设计，设计两个RL的Repositon Agent。请参考 @MobRef中的Agent设计，不要耦合 Agent 流程 环境，而是一般来说都是解耦的，流程里面可以调用BaseAgent的方法，而具体实现，RL Reposition 方法可能有实现，启发式Reposition可能留白即可。


请注意 @MobRef 是用于参考的项目代码，在现有项目的代码时可以进行参考，尤其是时训练/测试模式，与RL方法Agent的实现。
